# 2. Mastering Python Modules and Aliases

import text_utils as nloc # stands for needlessly overcomplicated

s = "if only there was a simple way to capitalize and reverse strings, already built into python"
print(nloc.capitalize_string(s))
print(nloc.reverse_string(s))